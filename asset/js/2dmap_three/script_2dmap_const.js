const test2 = 30;

// カメラ操作の有効化切り替え
const controlEnRotate = true;
const controlEnPan = true;

// カメラ操作の各スピード
const controlSpRotate = 0.5;
const controlSpZoom = 0.25;
const controlSpPan = 0.5;

// カメラの仰角の最大値/最小値
const controlMinAngle = 50;
const controlMaxAngle = 45;

// カメラの最大距離/最小距離
const controlMinDistance = 3;
const controlMaxDistance = 9;

// カメラの滑らかな動きの有効化切り替え
const controlEnDamping = true;

// カメラの滑らかな動きの係数
const controlDampingFactor = 0.15;